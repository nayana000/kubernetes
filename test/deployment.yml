apiVersion: apps/v1
kind: Deployment
metadata:
  name: testdeploy
spec:
  replicas: 2
  selector:
    app: myapp
  template:
    metadata:
      labels:
        app: myapp
  spec:
    container:
      - name: nginx
        image: nginx
        ports:
          - containerPort: 80